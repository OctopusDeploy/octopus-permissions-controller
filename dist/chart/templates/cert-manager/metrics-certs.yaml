{{- if and .Values.certManager.enable .Values.metrics.enable .Values.metrics.secure }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "octopus-permissions-controller.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "octopus-permissions-controller.resourceName" (dict "suffix" "metrics-certs" "context" $) }}
  namespace: {{ .Release.Namespace }}
spec:
  dnsNames:
  - {{ include "octopus-permissions-controller.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ include "octopus-permissions-controller.namespaceName" $ }}.svc
  - {{ include "octopus-permissions-controller.resourceName" (dict "suffix" "controller-manager-metrics-service" "context" $) }}.{{ include "octopus-permissions-controller.namespaceName" $ }}.svc.cluster.local
  issuerRef:
    kind: Issuer
    name: {{ include "octopus-permissions-controller.resourceName" (dict "suffix" "selfsigned-issuer" "context" $) }}
  secretName: metrics-server-cert
{{- end }}
