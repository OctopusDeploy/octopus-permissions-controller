{{- if .Values.metrics.enable }}
apiVersion: v1
kind: Service
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: octopus-permissions-controller
        control-plane: controller-manager
    name: opc-metrics-service
    namespace: opc-system
spec:
    ports:
        - name: https
          port: 8443
          protocol: TCP
          targetPort: 8443
    selector:
        app.kubernetes.io/name: octopus-permissions-controller
        control-plane: controller-manager
{{- end }}
